services:
  manager-app:
    build:
      context: .
      dockerfile: manager_app/Dockerfile
    container_name: revature-expense-manager
    ports:
      - "8080:8080"
    volumes:
      - ./db:/app/db
    restart: unless-stopped
